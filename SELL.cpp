#include <vector>

class SELL {
    public:

        SELL(std::vector<std::vector<double>> matrix, int sliceSize) {
            int nslices = matrix[0].size() / sliceSize;
            // store slices in colum major order
            
        }
};